# Internationalisation (i18n)

## Vue d'ensemble

Cookie Consent supporte nativement **7 langues européennes** avec détection automatique de la langue du navigateur.

## Langues supportées

| Langue | Code | Statut | Couverture |
|--------|------|--------|------------|
| 🇫🇷 Français | `fr` | ✅ Complet | 100% |
| 🇬🇧 English | `en` | ✅ Complet | 100% |
| 🇪🇸 Español | `es` | ✅ Complet | 100% |
| 🇩🇪 Deutsch | `de` | ✅ Complet | 100% |
| 🇮🇹 Italiano | `it` | ✅ Complet | 100% |
| 🇳🇱 Nederlands | `nl` | ✅ Complet | 100% |
| 🇵🇹 Português | `pt` | ✅ Complet | 100% |

## Détection automatique

Par défaut, Cookie Consent détecte la langue via `navigator.language` :

```javascript
// Détection automatique
const userLang = navigator.language || navigator.userLanguage;
// Exemples : 'fr-FR', 'en-US', 'es-ES', 'de-DE'

// Extraction du code langue (2 lettres)
const lang = userLang.split('-')[0]; // 'fr', 'en', 'es'...
```

**Aucune configuration nécessaire !**

## Forcer une langue

### Méthode 1 : Via l'API JavaScript

```javascript
import t from '@synapxlab/cookie-consent/translat';

// Forcer l'anglais
t.setLocale('en');

// Initialiser Cookie Consent
window.CookieConsent.init({
  statistics: {
    google_analytics_key: 'G-XXXXXXXXX'
  }
});
```

### Méthode 2 : Selon la langue du site

```javascript
// Détecter la langue de votre site (HTML lang attribute)
const siteLang = document.documentElement.lang.split('-')[0];

import t from '@synapxlab/cookie-consent/translat';
t.setLocale(siteLang);
```

### Méthode 3 : Choix utilisateur

```html
<!-- Sélecteur de langue -->
<select id="language-selector">
  <option value="fr">Français</option>
  <option value="en">English</option>
  <option value="es">Español</option>
  <option value="de">Deutsch</option>
  <option value="it">Italiano</option>
  <option value="nl">Nederlands</option>
  <option value="pt">Português</option>
</select>

<script>
import t from '@synapxlab/cookie-consent/translat';

document.getElementById('language-selector').addEventListener('change', function(e) {
  const lang = e.target.value;
  
  // Changer la langue
  t.setLocale(lang);
  
  // Recharger la bannière si déjà affichée
  if (document.getElementById('politecookiebanner')) {
    window.CookieConsent.reset();
  }
  
  // Sauvegarder le choix
  localStorage.setItem('userLang', lang);
});

// Restaurer le choix au chargement
const savedLang = localStorage.getItem('userLang');
if (savedLang) {
  t.setLocale(savedLang);
}
</script>
```

## Ajouter une nouvelle langue

### Structure de traduction

Toutes les clés de traduction disponibles :

```javascript
import t from '@synapxlab/cookie-consent/translat';

t.add('CODE_LANGUE', {
  // Header
  title: "Titre de la bannière",
  closeAria: "Texte accessible pour fermer",
  
  // Message principal
  message: "Message d'introduction sur les cookies",
  
  // Boutons
  acceptAll: "Tout accepter",
  denyAll: "Tout refuser",
  viewPrefs: "Personnaliser mes choix",
  savePrefs: "Enregistrer mes préférences",
  delPrefs: "Supprimer mes préférences",
  
  // Statut
  alwaysActive: "Toujours actif",
  
  // Catégorie : Fonctionnel
  functionalTitle: "Cookies strictement nécessaires",
  functionalDesc: "Description des cookies fonctionnels...",
  
  // Catégorie : Cookies (fonctionnels additionnels)
  cookiesTitle: "Cookies pour fonctionnalités",
  cookiesDesc: "Description des cookies fonctionnels additionnels...",
  cookiesServices: "Services : {services}",
  
  // Catégorie : Statistiques
  statsTitle: "Cookies de mesure d'audience",
  statsDesc: "Description des cookies statistiques...",
  statsServices: "Services : {services}",
  
  // Catégorie : Marketing
  marketingTitle: "Cookies publicitaires",
  marketingDesc: "Description des cookies marketing...",
  marketingServices: "Services : {services}",
  
  // Logging RGPD
  loggingTitle: "📋 Preuve de consentement",
  loggingNotice: "Information sur la journalisation des consentements..."
});
```

### Exemple : Ajouter le japonais

```javascript
import t from '@synapxlab/cookie-consent/translat';

t.add('ja', {
  title: "クッキー同意管理",
  closeAria: "クッキーバナーを閉じる",
  message: "最高のエクスペリエンスを提供するため、当サイトはクッキーを使用して利用状況を分析し、サービスを改善しています。",
  
  acceptAll: "すべて受け入れる",
  denyAll: "すべて拒否",
  viewPrefs: "設定をカスタマイズ",
  savePrefs: "設定を保存",
  delPrefs: "設定を削除",
  
  alwaysActive: "常に有効",
  
  functionalTitle: "必須クッキー",
  functionalDesc: "これらのクッキーはウェブサイトの動作に不可欠であり、システム内で無効にすることはできません。通常、プライバシー設定、ログイン、フォーム入力などのアクションに応じて設定されます。",
  
  cookiesTitle: "機能的クッキー",
  cookiesDesc: "これらのクッキーはウェブサイトの機能を向上させ、カスタマイズします。",
  cookiesServices: "サービス：{services}",
  
  statsTitle: "統計クッキー（アクセス解析）",
  statsDesc: "これらのクッキーは訪問者数の測定、統計の作成、トラフィック経路の分析を可能にし、サービスのパフォーマンスと品質を向上させます。",
  statsServices: "サービス：{services}",
  
  marketingTitle: "広告・コンテンツパーソナライゼーションクッキー",
  marketingDesc: "これらのクッキーはウェブサイト上での行動に基づいて、興味に合わせた広告をカスタマイズします。",
  marketingServices: "サービス：{services}",
  
  loggingTitle: "📋 クッキー同意の証明",
  loggingNotice: "法的要件（GDPR第7条第1項）およびCNILガイドラインに従い、同意設定の記録を保持します..."
});

// Activer le japonais
t.setLocale('ja');
```

### Exemple : Ajouter l'arabe (RTL)

```javascript
t.add('ar', {
  title: "إدارة موافقة ملفات تعريف الارتباط",
  closeAria: "إغلاق لافتة ملفات تعريف الارتباط",
  message: "لتقديم أفضل تجربة، يستخدم هذا الموقع ملفات تعريف الارتباط لتحليل الاستخدام وتحسين خدماتنا.",
  
  acceptAll: "قبول الكل",
  denyAll: "رفض الكل",
  viewPrefs: "تخصيص تفضيلاتي",
  savePrefs: "حفظ التفضيلات",
  delPrefs: "حذف التفضيلات",
  
  alwaysActive: "نشط دائماً",
  
  functionalTitle: "ملفات تعريف الارتباط الضرورية للغاية",
  functionalDesc: "هذه الملفات ضرورية لتشغيل الموقع ولا يمكن تعطيلها في أنظمتنا...",
  
  cookiesTitle: "ملفات تعريف الارتباط الوظيفية",
  cookiesDesc: "تساعد هذه الملفات في تحسين وظائف الموقع وتخصيصها...",
  cookiesServices: "الخدمات: {services}",
  
  statsTitle: "ملفات تعريف الارتباط الإحصائية",
  statsDesc: "تساعدنا هذه الملفات في قياس عدد الزوار وتحليل استخدام الموقع...",
  statsServices: "الخدمات: {services}",
  
  marketingTitle: "ملفات تعريف الارتباط الإعلانية",
  marketingDesc: "تستخدم هذه الملفات لتخصيص الإعلانات بناءً على اهتماماتك...",
  marketingServices: "الخدمات: {services}",
  
  loggingTitle: "📋 إثبات الموافقة",
  loggingNotice: "وفقًا للمتطلبات القانونية..."
});

t.setLocale('ar');

// Support RTL (Right-to-Left)
document.documentElement.setAttribute('dir', 'rtl');
```

## Fichier de traduction externe

### Structure JSON

Créer un fichier `translations.json` :

```json
{
  "ja": {
    "title": "クッキー同意管理",
    "message": "最高のエクスペリエンスを...",
    "acceptAll": "すべて受け入れる",
    "denyAll": "すべて拒否"
  },
  "zh": {
    "title": "Cookie同意管理",
    "message": "为了提供最佳体验...",
    "acceptAll": "全部接受",
    "denyAll": "全部拒绝"
  }
}
```

### Charger les traductions

```javascript
import t from '@synapxlab/cookie-consent/translat';

// Charger le JSON
fetch('/translations.json')
  .then(response => response.json())
  .then(translations => {
    // Ajouter chaque langue
    Object.keys(translations).forEach(lang => {
      t.add(lang, translations[lang]);
    });
    
    // Activer la langue souhaitée
    t.setLocale('ja');
    
    // Initialiser Cookie Consent
    window.CookieConsent.init({
      statistics: {
        google_analytics_key: 'G-XXX'
      }
    });
  });
```

## Intégration avec frameworks

### React

```jsx
// i18n-setup.js
import t from '@synapxlab/cookie-consent/translat';

export const setupCookieConsentI18n = (language) => {
  t.setLocale(language);
};

// App.jsx
import { useEffect } from 'react';
import { useTranslation } from 'react-i18next'; // Votre i18n
import '@synapxlab/cookie-consent';
import { setupCookieConsentI18n } from './i18n-setup';

function App() {
  const { i18n } = useTranslation();
  
  useEffect(() => {
    // Synchroniser Cookie Consent avec votre i18n
    setupCookieConsentI18n(i18n.language);
  }, [i18n.language]);
  
  return <div>Your App</div>;
}
```

### Vue.js

```javascript
// plugins/cookie-consent.js
import t from '@synapxlab/cookie-consent/translat';

export default {
  install(app) {
    // Synchroniser avec Vue I18n
    const i18n = app.config.globalProperties.$i18n;
    
    app.config.globalProperties.$initCookieConsent = () => {
      t.setLocale(i18n.locale);
    };
    
    // Watch i18n changes
    watch(() => i18n.locale, (newLocale) => {
      t.setLocale(newLocale);
    });
  }
};

// main.js
import { createApp } from 'vue';
import { createI18n } from 'vue-i18n';
import cookieConsentPlugin from './plugins/cookie-consent';

const i18n = createI18n({
  locale: 'fr',
  messages: {
    fr: { /* ... */ },
    en: { /* ... */ }
  }
});

const app = createApp(App);
app.use(i18n);
app.use(cookieConsentPlugin);
app.mount('#app');
```

### Next.js

```javascript
// pages/_app.js
import { useEffect } from 'react';
import { useRouter } from 'next/router';
import t from '@synapxlab/cookie-consent/translat';
import '@synapxlab/cookie-consent';

function MyApp({ Component, pageProps }) {
  const router = useRouter();
  const { locale } = router;
  
  useEffect(() => {
    // Synchroniser avec Next.js locale
    t.setLocale(locale);
  }, [locale]);
  
  return <Component {...pageProps} />;
}

export default MyApp();
```

## Variables de remplacement

Certaines traductions supportent des **variables dynamiques** :

```javascript
// Dans les traductions
statsServices: "Services : {services}"
marketingServices: "Services : {services}"
cookiesServices: "Services : {services}"
```

Ces variables sont automatiquement remplacées par la liste des services configurés :

```javascript
window.CookieConsent.init({
  statistics: {
    google_analytics_key: 'G-XXX',
    hotjar_site_id: '123456'
  }
});

// Résultat affiché :
// "Services : Google Analytics, Hotjar"
```

## Support RTL (Right-to-Left)

Pour les langues qui s'écrivent de droite à gauche (arabe, hébreu, etc.) :

```javascript
// Ajouter la langue
t.add('ar', { /* traductions */ });
t.setLocale('ar');

// Activer le mode RTL
document.documentElement.setAttribute('dir', 'rtl');
document.documentElement.setAttribute('lang', 'ar');
```

### CSS RTL

Le module supporte automatiquement RTL avec CSS :

```css
/* CSS s'adapte automatiquement */
[dir="rtl"] #politecookiebanner {
  text-align: right;
}

[dir="rtl"] .pmcpli-buttons {
  flex-direction: row-reverse;
}
```

## Tests i18n

### Vérifier une traduction

```javascript
import t from '@synapxlab/cookie-consent/translat';

// Récupérer une clé
console.log(t('title')); // "Cookie Consent Management"

// Changer de langue
t.setLocale('fr');
console.log(t('title')); // "Gestion des cookies"

// Vérifier la langue active
console.log(t.getLocale()); // "fr"
```

### Tester toutes les langues

```javascript
const languages = ['fr', 'en', 'es', 'de', 'it', 'nl', 'pt'];

languages.forEach(lang => {
  t.setLocale(lang);
  console.log(`${lang}: ${t('title')}`);
});

// Résultat :
// fr: Gestion des cookies
// en: Cookie Consent Management
// es: Gestión de cookies
// de: Cookie-Verwaltung
// ...
```

## Fallback

Si une traduction est manquante, Cookie Consent utilise l'anglais par défaut :

```javascript
t.add('custom', {
  title: "Mon titre personnalisé"
  // Autres clés manquantes
});

t.setLocale('custom');

console.log(t('title')); // "Mon titre personnalisé"
console.log(t('acceptAll')); // "Accept all" (fallback anglais)
```

## Contribution

Vous voulez ajouter une langue ? Contribuez sur GitHub !

1. **Fork** le repository
2. **Créer** un fichier `src/js/locales/[CODE].js`
3. **Ajouter** toutes les traductions
4. **Tester** avec `npm run test:i18n`
5. **Pull Request** avec captures d'écran

### Template de contribution

```javascript
// src/js/locales/ja.js
export default {
  title: "クッキー同意管理",
  closeAria: "クッキーバナーを閉じる",
  message: "...",
  acceptAll: "...",
  denyAll: "...",
  viewPrefs: "...",
  savePrefs: "...",
  delPrefs: "...",
  alwaysActive: "...",
  functionalTitle: "...",
  functionalDesc: "...",
  cookiesTitle: "...",
  cookiesDesc: "...",
  cookiesServices: "...",
  statsTitle: "...",
  statsDesc: "...",
  statsServices: "...",
  marketingTitle: "...",
  marketingDesc: "...",
  marketingServices: "...",
  loggingTitle: "...",
  loggingNotice: "..."
};
```

## Ressources

### Langues en cours d'ajout

- 🇯🇵 Japonais (ja)
- 🇨🇳 Chinois simplifié (zh)
- 🇰🇷 Coréen (ko)
- 🇷🇺 Russe (ru)
- 🇸🇦 Arabe (ar)
- 🇮🇱 Hébreu (he)
- 🇮🇳 Hindi (hi)

**Vous parlez une de ces langues ?** Contribuez sur GitHub !

### Support

- 📧 contact@synapxlab.com
- 💬 [Discord SynapxLab](https://discord.gg/synapxlab)
- 🐛 [GitHub Issues](https://github.com/synapxLab/cookie-consent/issues)
- 📚 [Documentation complète](../configuration.md)

---

**Guide i18n by SynapxLab**
**7 langues supportées**
**Open source - Contributions bienvenues !**